__pycache__/
*.pyc
*.egg-info/
.venv/
venv/
.env
.mypy_cache/
.pytest_cache/
.ruff_cache/

# repos cloned locally as demo/analysis targets, not vendored
data/repos/

# generated output, regenerate with scripts/visualize.py
data/*.html

# regenerable embedding cache, keyed by local absolute path anyway
data/embedding_cache/

.ipynb_checkpoints/
.DS_Store

# machine-specific absolute paths, see .mcp.json.example
.mcp.json

# local MCP approval state and other personal Claude Code state, not shared
.claude/
